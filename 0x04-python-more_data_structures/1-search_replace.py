#!/usr/bin/python3
def search_replace(my_list, search, replace):
    my_list = [search, food, string]
    new_list = my_list.copy()
    new_list = list(map(lambda x: replace if x == search else, new_list))
    return (new_list)
